{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna clean --yes",
    "init": "lerna bootstrap --hoist",
    "compile": "lerna run compile",
    "test": "lerna run test",
    "test:local": "littletravis"
  },
  "devDependencies": {
    "lerna": "^3.3.0",
    "littletravis": "^0.0.2"
  }
}
